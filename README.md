# KION
Видеоаналитика фильмов  
Текущий проект создан для первичной аналитики фильмов: разбиения фильма на сцены, трекинг актеров, 
определение частоты монтажа и определение динамики сцен.

## Описание проекта  
В проекте реализовано 2 алгоритма трекинга актеров в кадре и разбиения файла на шоты, а также объединение шотов в сцены на
основе анализа аудиодорожки и субтитров. Дополнительно сохраняются шоты с разметкой трекинга актеров для удобства оценки результата пользователем.
Также результатом работы алгоритма является набор файлов с отчетами: разбиение на шоты, 
разбиение на сцены, трекинг актеров внутри шотов.  
Визуализация результатов доступна в отдельном файле jupiter.  
Пример размеченного шота с трекингом актеров:  
![](docs/input_debug_2.gif)


### Использование ресурсов и производительность

Оценка производительности производилась на обработке фильма "Мистер и миссис Смит" с разрешением 720х304 и
полной длительностью 7198с, запакованный видеокодеком H264 и аудиокодеком aac.  
Для расчета затраченного времени и использования ресурсов был использован ПК с характеристиками:
- процессор -  AMD Ryzen9 7940HX,
- память -  32 Гб DDR5,
- видеокарта - RTX4070 Laptop.  

При обработке фильма основным алгоритмом на основе YOLO8 трекера объектов и анализатора шотов pyscene было
затрачено ???, при обработке фильма трекером собственной разработки с детектором объектов на базе YOLO8
было затрачено 19392с.  
Загрузка процессора и видекарты в обоих случаях была примерно одинаковой:
![](docs/cpu_utilization.jpg)  
Загрузка как процессора, так и видеокарты была незначительной, что связано с тем, что процесс обработки видео
плохо разбивается на параллельные процессы. Но этот факт говорит о том, что можно запустить на ПК несколько задач 
обработки фильмов без заметного увеличения времени обработки каждого из них.

## Разворачивание окружения  
Было использовано окружение conda с python3.9.
Для работы pytorch на мощностях видеокарты было установлено окружение NVIDIA CUDA 11.8 и библиотеки cuDnn 9.11.
Не следует использовать виртуальные среды venv, если нужна поддержка GPU ускорителя в библиотеке dlib 
(в них имеются трудности сборки dlib с поддержкой CUDA). 
В текущем проекте было использовано виртуальное окружение conda.

### Последовательность установки  
#### Установка в виртуальное окружение
Для установки используемых библиотек нужных версий выполните команду в рабочей папке проекта:
pip install -r requirements.txt.
Отдельно установите pytorch, исходя из особенной платформы, на которой разворачивается проект.
Установка pytorch:
Перейдите на сайт https://pytorch.org/get-started/locally/ и выберите ссылку "install previous versions of PyTorch":
![img.png](docs/img.png)  
На открывшейся странице нужно выбрать подходящий вариант установки PyTorch2.0.1 (в зависимости от имеющейся ОС и версии CUDA).
Далее нужно установить библиотеку Dlib. Возможна установка библиотеки только с поддержкой CPU командой  
`pip install dlib`  
Но при наличии GPU ускорителя лучше установить dlib с поддержкой GPU.  
Для этого нужно  выполнить следующую команду:  
```conda install conda-forge::dlib```  
Для установки поддержки работы с субтитрами нужна библиотека spacy.
Лучше всего установить его следующей командой:
```conda install conda-forge::spacy```  
После этого нужно установить поддержку русского языка для этой библиотеки командой:
```python -m spacy download ru_core_news_md```
#### Установка в докер


## Запуск проекта  
Запуск анализа видео запускается командой:  
`python shorts.py --video \<input video> [--output_dir /data][-base_update]`  
Результатом работы будет набор файлов шотов фильма в папке /data/shots, набор файлов сцен в папке /data/scenes,
набор файлов шотов с разметкой актеров в папке /data/shaped_shorts, а также таблицы с данными трекера по каждому шоту
в папке /data/shaped_shorts. Если выходная директория не задана, она создается в папке с исходным файлом.  
Флаг -base_update в команде принудительно запускает обновление базы данных дескрипторов лиц актеров. Требуется указывать его
при изменении датасета лиц актеров (после добавления или удаления изображений).

## Запуск проекта в Jupiter  
Один из вариантов алгоритма полностью реализован в Jupiter в файле "hakaton3_0.ipynb". В этом блокноте
реализован простой анализ субтитров, без привлечения нейросетевых моделей.
В файле jupiter "subs.ipynb" реализован анализ и кластеризация субтитров по сценам на основе nlp нейросетевой модели.


### Отчеты  
Для визуализации результатов (графики, отчеты) необходимо использовать блокнот Jupiter "visualization2.ipynb".


